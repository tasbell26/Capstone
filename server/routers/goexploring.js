// routers are always plural and representing the route your using
const { Router } = require("express");
const Pizza = require("../models/Goexplorings");

const router = Router();

module.exports = router;
